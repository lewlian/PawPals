---
phase: 03-core-check-in-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/0003-sessions.sql
  - src/types/session.ts
  - src/db/repositories/sessionRepository.ts
  - src/bot/utils/geofence.ts
autonomous: true

must_haves:
  truths:
    - "Check-in sessions can be created and tracked in the database"
    - "Multi-dog check-ins are supported (multiple dogs per session)"
    - "User location is validated against known dog run locations"
    - "Nearest dog run is identified when user shares location"
  artifacts:
    - path: "src/db/migrations/0003-sessions.sql"
      provides: "Sessions and session_dogs tables with indexes"
      contains: "CREATE TABLE IF NOT EXISTS sessions"
    - path: "src/types/session.ts"
      provides: "Session and CheckInWizardState types"
      exports: ["Session", "CheckInWizardState"]
    - path: "src/db/repositories/sessionRepository.ts"
      provides: "Session CRUD operations"
      exports: ["createSession", "getActiveSessionByUserId", "checkoutSession", "findSessionById"]
    - path: "src/bot/utils/geofence.ts"
      provides: "Geofence validation using Haversine"
      exports: ["validateGeofence", "GEOFENCE_RADIUS_METERS"]
  key_links:
    - from: "src/db/repositories/sessionRepository.ts"
      to: "src/db/client.ts"
      via: "pool import"
      pattern: "import.*pool.*from.*client"
    - from: "src/bot/utils/geofence.ts"
      to: "src/db/locations.ts"
      via: "getAllLocations import"
      pattern: "import.*getAllLocations.*from.*locations"
---

<objective>
Create the data layer for check-in sessions including database schema, TypeScript types, repository functions, and geofence utility.

Purpose: Establish the foundation for check-in/checkout functionality with proper session tracking and geofence validation.
Output: Migration file, session types, session repository, and geofence utility ready for the check-in wizard.
</objective>

<execution_context>
@/Users/sean/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-check-in-out/03-RESEARCH.md

Existing patterns:
@src/db/migrations/0002-users-dogs.sql
@src/db/repositories/dogRepository.ts
@src/db/locations.ts
@src/types/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sessions migration and install haversine-distance</name>
  <files>src/db/migrations/0003-sessions.sql, package.json</files>
  <action>
1. Install haversine-distance package:
   ```bash
   npm install haversine-distance
   ```

2. Create migration file `src/db/migrations/0003-sessions.sql` with:
   - `sessions` table: id (SERIAL PK), user_id (FK to users), location_id (FK to locations), checked_in_at (TIMESTAMPTZ DEFAULT NOW()), expires_at (TIMESTAMPTZ NOT NULL), checked_out_at (TIMESTAMPTZ nullable), status (VARCHAR(20) with CHECK constraint for 'active', 'expired', 'completed'), created_at, updated_at
   - `session_dogs` junction table: id (SERIAL PK), session_id (FK to sessions ON DELETE CASCADE), dog_id (FK to dogs ON DELETE CASCADE), created_at, UNIQUE(session_id, dog_id)
   - Indexes: idx_sessions_user_active (user_id, status WHERE active), idx_sessions_expires_at (expires_at WHERE active), idx_sessions_location_active (location_id, status WHERE active), idx_session_dogs_session, idx_session_dogs_dog

Follow existing migration patterns (IF NOT EXISTS, matching timestamp types).
  </action>
  <verify>
Run `npm run db:migrate` - should execute without errors.
Query `\dt` in psql should show sessions and session_dogs tables.
  </verify>
  <done>Sessions and session_dogs tables created with all columns and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Update session types and create session repository</name>
  <files>src/types/session.ts, src/db/repositories/sessionRepository.ts</files>
  <action>
1. Update the existing `src/types/session.ts` file (which already has ProfileWizardState, SceneSessionData, SessionData, BotContext) to add:
   - `Session` interface: id, userId, locationId, checkedInAt (Date), expiresAt (Date), checkedOutAt (Date | null), status ('active' | 'expired' | 'completed'), createdAt, updatedAt
   - `CheckInWizardState` interface: locationId?, locationName?, selectedDogIds?, durationMinutes?, userId?, allDogIds?
   - Keep all existing exports (ProfileWizardState, SceneSessionData, SessionData, BotContext)

2. Create `src/db/repositories/sessionRepository.ts` following dogRepository.ts patterns with these specific functions:
   - `SessionRow` interface mapping DB columns to snake_case
   - `mapRowToSession()` helper function
   - `createSession(userId: number, locationId: number, durationMinutes: number): Promise<Session>` - inserts session, calculates expires_at as NOW() + durationMinutes
   - `getActiveSessionByUserId(userId: number): Promise<Session | null>` - gets latest session with status='active'
   - `checkoutSession(sessionId: number): Promise<Session | null>` - updates checked_out_at = NOW(), status = 'completed'
   - `findSessionById(sessionId: number): Promise<Session | null>` - retrieves single session by ID
   - `addDogsToSession(sessionId: number, dogIds: number[]): Promise<void>` - batch insert into session_dogs
   - `getDogsBySessionId(sessionId: number): Promise<Dog[]>` - joins session_dogs with dogs table
  </action>
  <verify>
TypeScript compiles without errors: `npx tsc --noEmit`
  </verify>
  <done>Session types exported, sessionRepository has all CRUD operations.</done>
</task>

<task type="auto">
  <name>Task 3: Create geofence utility</name>
  <files>src/bot/utils/geofence.ts</files>
  <action>
1. Create directory if needed: `src/bot/utils/`

2. Create `src/bot/utils/geofence.ts`:
   - Import `haversineDistance` from 'haversine-distance'
   - Import `getAllLocations, Location` from '../../db/locations.js'
   - Export constant `GEOFENCE_RADIUS_METERS = 200`
   - Define `GeofenceResult` interface: isWithinGeofence (boolean), nearestLocation? (Location), distance? (number in meters)
   - Export async function `validateGeofence(userLat: number, userLon: number): Promise<GeofenceResult>`:
     - Get all locations from DB
     - For each location, calculate haversine distance from user point
     - Track nearest location and minimum distance
     - Return { isWithinGeofence: minDistance <= GEOFENCE_RADIUS_METERS, nearestLocation, distance: minDistance }

Note: haversine-distance accepts { latitude, longitude } objects and returns distance in meters.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
Can be imported without errors in a test script.
  </verify>
  <done>Geofence utility validates 200m proximity and finds nearest dog run.</done>
</task>

</tasks>

<verification>
1. `npm run db:migrate` succeeds
2. `npx tsc --noEmit` passes with no errors
3. Tables exist: `psql -c "\dt" | grep -E "(sessions|session_dogs)"`
4. Package installed: `npm ls haversine-distance`
</verification>

<success_criteria>
- Sessions table has user_id, location_id, expires_at, status columns with proper constraints
- Session_dogs junction table links sessions to multiple dogs
- sessionRepository exports createSession, checkoutSession, getActiveSessionByUserId, addDogsToSession, getDogsBySessionId
- geofence.ts exports validateGeofence function using Haversine formula with 200m threshold
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-check-in-out/03-01-SUMMARY.md`
</output>
