---
phase: 03-core-check-in-out
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/bot/handlers/checkout.ts
  - src/bot/handlers/checkin.ts
  - src/bot/index.ts
autonomous: true

must_haves:
  truths:
    - "User can send /checkout to end active session"
    - "Checkout shows confirmation with session details"
    - "User without active session sees helpful message"
    - "Check-in wizard is registered with bot stage"
    - "/checkin command enters check-in wizard"
  artifacts:
    - path: "src/bot/handlers/checkout.ts"
      provides: "Functional checkout handler"
      exports: ["checkoutHandler"]
      min_lines: 40
    - path: "src/bot/handlers/checkin.ts"
      provides: "Handler that enters check-in wizard"
      exports: ["checkinHandler"]
    - path: "src/bot/index.ts"
      provides: "Bot with check-in wizard registered"
      contains: "checkInWizard"
  key_links:
    - from: "src/bot/handlers/checkout.ts"
      to: "src/db/repositories/sessionRepository.ts"
      via: "checkoutSession import"
      pattern: "import.*checkoutSession.*from.*sessionRepository"
    - from: "src/bot/index.ts"
      to: "src/bot/scenes/checkInWizard.ts"
      via: "checkInWizard import"
      pattern: "import.*checkInWizard.*from.*scenes/checkInWizard"
---

<objective>
Implement the /checkout command handler and integrate the check-in wizard with the bot, completing the check-in/checkout functionality.

Purpose: Enable users to manually end their sessions and wire up all check-in components to the bot.
Output: Working /checkout command and fully integrated check-in flow.
</objective>

<execution_context>
@/Users/sean/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-check-in-out/03-RESEARCH.md

Existing patterns:
@src/bot/handlers/profile.ts
@src/bot/index.ts
@src/bot/handlers/checkout.ts (placeholder)
@src/bot/handlers/checkin.ts (placeholder)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement checkout handler</name>
  <files>src/bot/handlers/checkout.ts</files>
  <action>
Replace the placeholder in `src/bot/handlers/checkout.ts` with functional implementation:

1. Import required modules:
   - `Context` from 'telegraf'
   - `getActiveSessionByUserId, checkoutSession, getDogsBySessionId` from '../../db/repositories/sessionRepository.js'
   - `findOrCreateUser` from '../../db/repositories/userRepository.js'
   - `getLocationById` from '../../db/locations.js'

2. Implement `checkoutHandler(ctx: Context)`:
   - Get telegramId from ctx.from?.id
   - If no telegramId: Reply error, return
   - Try/catch block:
     - Find or create user
     - Get active session via `getActiveSessionByUserId(user.id)`
     - If no active session: Reply "You are not currently checked in to any dog run. Use /checkin when at a dog run to start a session."
     - Get location details via `getLocationById(session.locationId)`
     - Get dogs in session via `getDogsBySessionId(session.id)`
     - Call `checkoutSession(session.id)` to end session
     - Calculate session duration: `Math.floor((Date.now() - session.checkedInAt.getTime()) / 60000)` minutes
     - Reply confirmation:
       ```
       Checked out successfully!

       Location: {location.name}
       Dog(s): {dog names joined}
       Session duration: {minutes} minutes

       Thanks for using PawPals SG!
       ```
   - Catch: Log error, reply generic error message
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
Handler exports checkoutHandler function.
  </verify>
  <done>Checkout handler ends active session and shows confirmation with details.</done>
</task>

<task type="auto">
  <name>Task 2: Update checkin handler and integrate wizard</name>
  <files>src/bot/handlers/checkin.ts, src/bot/index.ts</files>
  <action>
1. Update `src/bot/handlers/checkin.ts`:
   - Change import to use BotContext: `import type { BotContext } from '../../types/session.js'`
   - Update function signature: `export async function checkinHandler(ctx: BotContext): Promise<void>`
   - Replace placeholder body: `await ctx.scene.enter('check-in');`

2. Update `src/bot/index.ts`:
   - Add import: `import { checkInWizard } from './scenes/checkInWizard.js';`
   - Add checkInWizard to the Stage array (alongside createDogProfileWizard and editDogProfileWizard):
     ```typescript
     const stage = new Scenes.Stage<BotContext>([
       createDogProfileWizard,
       editDogProfileWizard,
       checkInWizard,
     ]);
     ```
   - Add a global 'location' handler for unexpected location messages (outside wizard):
     ```typescript
     bot.on('location', async (ctx) => {
       // Only handle if not in a scene
       if (!ctx.scene.current) {
         await ctx.reply(
           'To check in at a dog run, please use the /checkin command first.'
         );
       }
     });
     ```
     Place this AFTER stage middleware but BEFORE the 'text' handler.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
Bot starts without errors: `npm run dev` (test briefly, Ctrl+C)
  </verify>
  <done>/checkin enters wizard, wizard is registered, location messages outside wizard get helpful response.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `npm run dev` starts bot without errors (brief test)
3. /checkout shows appropriate message when not checked in
4. /checkin enters check-in wizard (shows location request)
5. Random location share (outside /checkin) shows helpful message
</verification>

<success_criteria>
- /checkout ends active session immediately (SESS-05, SESS-06)
- /checkout shows confirmation with location, dogs, and session duration
- /checkout shows helpful message when user has no active session
- /checkin enters the check-in wizard scene
- Check-in wizard is registered with bot stage
- Global location handler guides users to use /checkin command
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-check-in-out/03-03-SUMMARY.md`
</output>
