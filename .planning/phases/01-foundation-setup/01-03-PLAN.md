---
phase: 01-foundation-setup
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/index.ts
  - src/bot/index.ts
  - src/bot/handlers/start.ts
  - src/bot/handlers/profile.ts
  - src/bot/handlers/checkin.ts
  - src/bot/handlers/checkout.ts
  - src/bot/handlers/live.ts
autonomous: false

user_setup:
  - service: telegram
    why: "Bot token required for Telegram API"
    env_vars:
      - name: BOT_TOKEN
        source: "Telegram BotFather - create bot via /newbot command"
    dashboard_config:
      - task: "Create new bot"
        location: "Telegram -> @BotFather -> /newbot"
      - task: "Register commands"
        location: "Telegram -> @BotFather -> /setcommands"

must_haves:
  truths:
    - "User can send /start and receive welcome message with Create Profile button"
    - "User can send /profile and receive acknowledgment"
    - "User can send /checkin and receive acknowledgment"
    - "User can send /checkout and receive acknowledgment"
    - "User can send /live and receive acknowledgment"
    - "Bot handles errors gracefully without crashing"
  artifacts:
    - path: "src/index.ts"
      provides: "Application entry point"
      contains: "bot.launch"
    - path: "src/bot/index.ts"
      provides: "Telegraf bot configuration"
      exports: ["bot"]
    - path: "src/bot/handlers/start.ts"
      provides: "/start command handler"
      contains: "inline_keyboard"
  key_links:
    - from: "src/index.ts"
      to: "src/bot/index.ts"
      via: "bot import and launch"
      pattern: "import.*bot"
    - from: "src/bot/index.ts"
      to: "src/bot/handlers/start.ts"
      via: "handler registration"
      pattern: "bot\\.command"
    - from: "src/bot/handlers/start.ts"
      to: "ctx.reply"
      via: "inline_keyboard in reply_markup"
      pattern: "inline_keyboard"
---

<objective>
Implement the Telegram bot with all Phase 1 command handlers responding with appropriate messages.

Purpose: This establishes the bot's command structure (CMDS-01 through CMDS-05). Commands return placeholder messages now but will be fully implemented in later phases. The /start command must show an interactive "Create Dog Profile" button as specified in requirements.

Output: Working Telegram bot that responds to all 5 commands with the /start command showing an inline keyboard button.
</objective>

<execution_context>
@/Users/sean/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Telegraf bot with error handling and command handlers</name>
  <files>
    - src/bot/index.ts
    - src/bot/handlers/start.ts
    - src/bot/handlers/profile.ts
    - src/bot/handlers/checkin.ts
    - src/bot/handlers/checkout.ts
    - src/bot/handlers/live.ts
  </files>
  <action>
    1. Create src/bot/handlers/start.ts - /start command (CMDS-01):
       ```typescript
       import { Context } from 'telegraf';

       /**
        * /start command handler
        * Shows welcome message with "Create Dog Profile" button
        * Requirement: CMDS-01
        */
       export async function startHandler(ctx: Context): Promise<void> {
         const welcomeMessage = `Welcome to PawPals SG!

       Check real-time dog run occupancy across Singapore before making the trip.

       Get started by creating your dog's profile, then check in when you arrive at a dog run.

       Commands:
       /profile - Manage your dog profiles
       /checkin - Check in at a dog run
       /checkout - End your current session
       /live - View live occupancy`;

         await ctx.reply(welcomeMessage, {
           reply_markup: {
             inline_keyboard: [[
               { text: 'Create Dog Profile', callback_data: 'create_profile' }
             ]]
           }
         });
       }
       ```

    2. Create src/bot/handlers/profile.ts - /profile command (CMDS-02):
       ```typescript
       import { Context } from 'telegraf';

       /**
        * /profile command handler
        * Shows/edits dog profiles (placeholder for Phase 2)
        * Requirement: CMDS-02
        */
       export async function profileHandler(ctx: Context): Promise<void> {
         await ctx.reply(
           'Dog profile management will be available in the next update.\n\n' +
           'You will be able to:\n' +
           '- Add your dogs (name, size, breed, age)\n' +
           '- Manage multiple dog profiles\n' +
           '- Edit or delete profiles'
         );
       }
       ```

    3. Create src/bot/handlers/checkin.ts - /checkin command (CMDS-03):
       ```typescript
       import { Context } from 'telegraf';

       /**
        * /checkin command handler
        * Initiates check-in flow (placeholder for Phase 3)
        * Requirement: CMDS-03
        */
       export async function checkinHandler(ctx: Context): Promise<void> {
         await ctx.reply(
           'Check-in feature coming soon!\n\n' +
           'When available, you will:\n' +
           '1. Share your location\n' +
           '2. Select which dog(s) to check in\n' +
           '3. Choose your stay duration\n\n' +
           'First, create a dog profile using /profile'
         );
       }
       ```

    4. Create src/bot/handlers/checkout.ts - /checkout command (CMDS-05):
       ```typescript
       import { Context } from 'telegraf';

       /**
        * /checkout command handler
        * Ends current session (placeholder for Phase 3)
        * Requirement: CMDS-05
        */
       export async function checkoutHandler(ctx: Context): Promise<void> {
         await ctx.reply(
           'Checkout feature coming soon!\n\n' +
           'You are not currently checked in to any dog run.\n' +
           'Use /checkin when at a dog run to start a session.'
         );
       }
       ```

    5. Create src/bot/handlers/live.ts - /live command (CMDS-04):
       ```typescript
       import { Context } from 'telegraf';

       /**
        * /live command handler
        * Shows occupancy dashboard (placeholder for Phase 5)
        * Requirement: CMDS-04
        */
       export async function liveHandler(ctx: Context): Promise<void> {
         await ctx.reply(
           'Live dashboard coming soon!\n\n' +
           'When available, you will see:\n' +
           '- All 11 Singapore dog runs\n' +
           '- Current dog count at each park\n' +
           '- Size breakdown (Small/Medium/Large)\n' +
           '- Sort by most dogs or nearest to you'
         );
       }
       ```

    6. Create src/bot/index.ts - Main bot configuration:
       ```typescript
       import { Telegraf } from 'telegraf';
       import { validateEnv } from '../config/env.js';
       import { startHandler } from './handlers/start.js';
       import { profileHandler } from './handlers/profile.js';
       import { checkinHandler } from './handlers/checkin.js';
       import { checkoutHandler } from './handlers/checkout.js';
       import { liveHandler } from './handlers/live.js';

       const env = validateEnv();

       export const bot = new Telegraf(env.BOT_TOKEN);

       // Global error handler - prevents crashes from unhandled errors
       bot.catch((err, ctx) => {
         console.error(`Error for ${ctx.updateType}:`, err);
         ctx.reply('Sorry, something went wrong. Please try again.').catch(() => {
           // Silent fail if we can't send error message
         });
       });

       // Register command handlers
       bot.command('start', startHandler);
       bot.command('profile', profileHandler);
       bot.command('checkin', checkinHandler);
       bot.command('checkout', checkoutHandler);
       bot.command('live', liveHandler);

       // Handle "Create Dog Profile" button callback
       bot.action('create_profile', async (ctx) => {
         await ctx.answerCbQuery(); // Remove loading state
         await ctx.reply(
           'Profile creation will be available in the next update!\n\n' +
           'Use /profile to check when this feature becomes available.'
         );
       });

       // Handle unknown commands gracefully
       bot.on('text', async (ctx) => {
         const text = ctx.message.text;
         if (text.startsWith('/')) {
           await ctx.reply(
             'Unknown command. Available commands:\n' +
             '/start - Welcome message\n' +
             '/profile - Manage dog profiles\n' +
             '/checkin - Check in at dog run\n' +
             '/checkout - End session\n' +
             '/live - View live occupancy'
           );
         }
       });
       ```

    IMPORTANT:
    - Always call ctx.answerCbQuery() for callback handlers to remove loading spinner
    - Include bot.catch() to prevent crashes from unhandled errors
    - Use separate handler files for maintainability (Phase 2+ will expand these)
    - Register commands in BotFather separately (user setup step)
  </action>
  <verify>
    - `npx tsc --noEmit` compiles without errors
    - All handler files export their functions
    - bot/index.ts imports and registers all handlers
  </verify>
  <done>
    - All 5 command handlers created with appropriate placeholder messages
    - /start handler includes inline keyboard with "Create Dog Profile" button
    - create_profile callback handler responds with appropriate message
    - Global error handler prevents bot crashes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create application entry point with graceful shutdown</name>
  <files>
    - src/index.ts
  </files>
  <action>
    1. Create src/index.ts - Application entry point:
       ```typescript
       import { bot } from './bot/index.js';
       import { closePool, checkConnection } from './db/client.js';

       async function main(): Promise<void> {
         console.log('Starting PawPals SG bot...');

         // Verify database connection
         const dbConnected = await checkConnection();
         if (!dbConnected) {
           console.error('Failed to connect to database. Exiting.');
           process.exit(1);
         }
         console.log('Database connected');

         // Launch bot in polling mode (development)
         await bot.launch();
         console.log('Bot is running!');
         console.log('Press Ctrl+C to stop');
       }

       // Graceful shutdown handlers
       async function shutdown(signal: string): Promise<void> {
         console.log(`\n${signal} received. Shutting down gracefully...`);

         try {
           bot.stop(signal);
           console.log('Bot stopped');

           await closePool();
           console.log('Database pool closed');

           console.log('Shutdown complete');
           process.exit(0);
         } catch (error) {
           console.error('Error during shutdown:', error);
           process.exit(1);
         }
       }

       process.once('SIGINT', () => shutdown('SIGINT'));
       process.once('SIGTERM', () => shutdown('SIGTERM'));

       // Start the application
       main().catch((err) => {
         console.error('Failed to start bot:', err);
         process.exit(1);
       });
       ```

    IMPORTANT:
    - Verify database connection before launching bot
    - Use graceful shutdown to properly close bot and database pool
    - Use polling mode for development (webhook migration in Phase 6)
    - Log startup sequence for debugging
  </action>
  <verify>
    - `npx tsc --noEmit` compiles without errors
    - With valid BOT_TOKEN and database: `npm run dev` starts bot and shows "Bot is running!"
    - Ctrl+C triggers graceful shutdown
  </verify>
  <done>
    - src/index.ts launches bot with database connection check
    - Graceful shutdown handles SIGINT and SIGTERM
    - Console logs show startup and shutdown sequence
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Telegram bot with all Phase 1 commands responding appropriately:
    - /start shows welcome message with "Create Dog Profile" inline button
    - /profile, /checkin, /checkout, /live respond with placeholder messages
    - Button press on "Create Dog Profile" responds with message
  </what-built>
  <how-to-verify>
    1. Ensure you have a BOT_TOKEN from @BotFather (create via /newbot if needed)
    2. Create a .env file with your BOT_TOKEN
    3. Start PostgreSQL (Docker or local)
    4. Run migrations: `npm run db:migrate`
    5. Start the bot: `npm run dev`
    6. Open Telegram and find your bot
    7. Test each command:
       - Send /start - should show welcome message with button
       - Tap "Create Dog Profile" button - should respond (button loading should disappear)
       - Send /profile - should show placeholder about dog profiles
       - Send /checkin - should show placeholder about check-in
       - Send /checkout - should show "not checked in" message
       - Send /live - should show placeholder about dashboard
       - Send /unknown - should show list of available commands
  </how-to-verify>
  <resume-signal>Type "approved" if all commands respond correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Run these checks to verify the plan is complete:

1. **TypeScript compilation:**
   ```bash
   npx tsc --noEmit
   ```

2. **Start bot (requires BOT_TOKEN and PostgreSQL):**
   ```bash
   npm run dev
   ```

3. **Test in Telegram:**
   - All 5 commands respond with messages
   - /start shows inline button
   - Button press works (no stuck loading state)
   - Unknown commands show help message
</verification>

<success_criteria>
1. Bot launches successfully and connects to database
2. /start shows welcome message with "Create Dog Profile" inline button (CMDS-01)
3. /profile responds with dog profile placeholder (CMDS-02)
4. /checkin responds with check-in placeholder (CMDS-03)
5. /live responds with dashboard placeholder (CMDS-04)
6. /checkout responds with checkout placeholder (CMDS-05)
7. "Create Dog Profile" button works without stuck loading
8. Unknown commands show help text
9. Errors are handled gracefully (bot doesn't crash)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-03-SUMMARY.md`
</output>
