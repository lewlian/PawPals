---
phase: 04-session-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/repositories/sessionRepository.ts
  - src/bot/handlers/sessionCallbacks.ts
  - src/bot/index.ts
autonomous: true

must_haves:
  truths:
    - "Session can be extended by 15/30/60 minutes via inline button"
    - "Session can be checked out via inline button"
    - "Expired session extend attempt offers fresh check-in guidance"
  artifacts:
    - path: "src/db/repositories/sessionRepository.ts"
      provides: "Expiry queries and extend function"
      exports: ["getSessionsNeedingReminder", "getExpiredSessions", "expireSessions", "extendSession"]
    - path: "src/bot/handlers/sessionCallbacks.ts"
      provides: "Inline button callback handlers"
      exports: ["handleExtendCallback", "handleCheckoutCallback"]
  key_links:
    - from: "src/bot/handlers/sessionCallbacks.ts"
      to: "src/db/repositories/sessionRepository.ts"
      via: "extendSession and checkoutSession imports"
      pattern: "import.*from.*sessionRepository"
    - from: "src/bot/index.ts"
      to: "src/bot/handlers/sessionCallbacks.ts"
      via: "bot.action() registration"
      pattern: "bot\\.action\\(/\\^extend_"
---

<objective>
Add session expiry database queries and inline keyboard callback handlers for extend/checkout buttons.

Purpose: Provide the data layer and user interaction handlers needed for session automation. Users will tap buttons from reminder notifications to extend their session or check out early.

Output: Extended sessionRepository with expiry queries, new sessionCallbacks handler file, callback registrations in bot/index.ts
</objective>

<execution_context>
@/Users/sean/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-automation/04-CONTEXT.md
@.planning/phases/04-session-automation/04-RESEARCH.md
@src/db/repositories/sessionRepository.ts
@src/bot/index.ts
@src/types/session.ts
@src/db/locations.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add expiry queries to session repository</name>
  <files>src/db/repositories/sessionRepository.ts</files>
  <action>
Add the following to sessionRepository.ts:

1. Add interface `SessionForNotification` with fields: id, userId, telegramId (number), locationId, locationName, checkedInAt, expiresAt, dogNames (string[])

2. Add `getSessionsNeedingReminder()`: Returns active sessions where expires_at is between NOW() and NOW() + 6 minutes. Join with users (for telegram_id), locations (for name), session_dogs, and dogs (for dog names). Use ARRAY_AGG for dog names. Map telegram_id from string to number.

3. Add `getExpiredSessions()`: Returns active sessions where expires_at <= NOW(). Same joins and mapping as above.

4. Add `expireSessions(sessionIds: number[])`: Batch update sessions to status='expired', updated_at=NOW(). Use `WHERE id = ANY($1)`. Return early if empty array.

5. Add `extendSession(sessionId: number, additionalMinutes: number)`: UPDATE expires_at = expires_at + ($2 || ' minutes')::INTERVAL, updated_at = NOW() WHERE id = $1 AND status = 'active'. Return updated Session or null.

Export all new functions. Follow existing patterns in the file (mapRowToSession, pool.query).
  </action>
  <verify>
TypeScript compiles: `npm run build`
No type errors in sessionRepository.ts
  </verify>
  <done>
sessionRepository.ts exports: getSessionsNeedingReminder, getExpiredSessions, expireSessions, extendSession
All functions use correct PostgreSQL syntax and match existing patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Create session callback handlers</name>
  <files>src/bot/handlers/sessionCallbacks.ts</files>
  <action>
Create new file src/bot/handlers/sessionCallbacks.ts:

1. Import Markup from telegraf, BotContext from types/session.js, session repository functions (findSessionById, extendSession, checkoutSession, getDogsBySessionId), getLocationById from locations.js

2. `handleExtendCallback(ctx: BotContext)`:
   - Call ctx.answerCbQuery()
   - Parse sessionId and minutes from ctx.match (pattern: extend_123_15)
   - Validate session exists and status === 'active'
   - If session not found: editMessageText("This session no longer exists.")
   - If session expired/completed: editMessageText("This session has already ended.\n\nWant to start a new session? Use /checkin at a dog run!")
   - Call extendSession(sessionId, minutes)
   - If extend failed: editMessageText("Could not extend session. Please try /checkin to start a new session.")
   - Format new expiry time with toLocaleTimeString('en-SG', { hour: '2-digit', minute: '2-digit', hour12: true })
   - Get location name via getLocationById
   - editMessageText with success: "Session extended by {minutes} minutes!\n\nLocation: {name}\nNew end time: {time}\n\nI'll remind you again before it expires."

3. `handleCheckoutCallback(ctx: BotContext)`:
   - Call ctx.answerCbQuery()
   - Parse sessionId from ctx.match (pattern: checkout_123)
   - Validate session exists and is active
   - Get location and dogs before checkout
   - Call checkoutSession(sessionId)
   - Calculate duration in minutes: Math.round((Date.now() - session.checkedInAt.getTime()) / 60000)
   - editMessageText with confirmation: "Checked out successfully!\n\nLocation: {name}\nDog(s): {dogNames}\nDuration: {duration} minutes\n\nSee you next time!"

Export both handlers.
  </action>
  <verify>
TypeScript compiles: `npm run build`
File exists at src/bot/handlers/sessionCallbacks.ts
  </verify>
  <done>
sessionCallbacks.ts exports handleExtendCallback and handleCheckoutCallback
Both handlers validate session status before action
Both handlers use ctx.editMessageText for in-place message updates
  </done>
</task>

<task type="auto">
  <name>Task 3: Register callback handlers in bot</name>
  <files>src/bot/index.ts</files>
  <action>
Modify src/bot/index.ts:

1. Add import: `import { handleExtendCallback, handleCheckoutCallback } from './handlers/sessionCallbacks.js';`

2. After the existing callback registrations (after the location handler, before the unknown command handler), add:

```typescript
// Session automation callback handlers
bot.action(/^extend_(\d+)_(\d+)$/, handleExtendCallback);
bot.action(/^checkout_(\d+)$/, handleCheckoutCallback);
```

The regex patterns match:
- extend_123_15 (session ID, minutes to extend)
- checkout_123 (session ID)
  </action>
  <verify>
TypeScript compiles: `npm run build`
Bot starts without errors: `npm run dev` (Ctrl+C to stop)
  </verify>
  <done>
bot/index.ts imports sessionCallbacks handlers
bot.action() registered for extend_ and checkout_ patterns
Bot compiles and starts successfully
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npm run build`
2. Verify sessionRepository exports new functions
3. Verify sessionCallbacks.ts exists with both handlers
4. Verify bot/index.ts has callback registrations
5. Bot starts successfully: `npm run dev` (brief test, Ctrl+C)
</verification>

<success_criteria>
- sessionRepository.ts has 4 new exported functions for expiry handling
- sessionCallbacks.ts exists with extend and checkout callback handlers
- bot/index.ts registers both callback patterns
- TypeScript compiles with no errors
- Bot starts without crashing
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-automation/04-01-SUMMARY.md`
</output>
