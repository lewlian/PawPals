---
phase: 05-live-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/repositories/sessionRepository.ts
  - src/types/dashboard.ts
  - src/bot/utils/dashboard.ts
  - src/bot/handlers/live.ts
autonomous: true

must_haves:
  truths:
    - "User sends /live and sees all 11 dog runs with occupancy counts"
    - "Each park entry shows size breakdown in abbreviated format"
    - "Parks are sorted by most dogs (descending) by default"
    - "Dashboard shows timestamp of when data was retrieved"
    - "Parks with 0 dogs still appear in the list"
  artifacts:
    - path: "src/db/repositories/sessionRepository.ts"
      provides: "getOccupancyByLocation() aggregation query"
      exports: ["getOccupancyByLocation"]
    - path: "src/types/dashboard.ts"
      provides: "Dashboard type definitions"
      exports: ["OccupancyData", "ParkDisplay"]
    - path: "src/bot/utils/dashboard.ts"
      provides: "Dashboard formatting utilities"
      exports: ["formatSizeBreakdown", "formatDashboard", "sortByDogCount"]
    - path: "src/bot/handlers/live.ts"
      provides: "Live command handler with dashboard display"
      exports: ["liveHandler"]
  key_links:
    - from: "src/bot/handlers/live.ts"
      to: "src/db/repositories/sessionRepository.ts"
      via: "getOccupancyByLocation import and call"
      pattern: "getOccupancyByLocation\\(\\)"
    - from: "src/bot/handlers/live.ts"
      to: "src/bot/utils/dashboard.ts"
      via: "formatDashboard import and call"
      pattern: "formatDashboard\\("
---

<objective>
Implement core live dashboard showing all 11 Singapore dog runs with real-time occupancy data and size breakdowns.

Purpose: Enable users to see current park occupancy before visiting, addressing the core value proposition of the app.
Output: Working /live command that displays aggregated session data with proper formatting.
</objective>

<execution_context>
@/Users/sean/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-dashboard/05-CONTEXT.md
@.planning/phases/05-live-dashboard/05-RESEARCH.md

# Key source files to reference
@src/db/repositories/sessionRepository.ts
@src/bot/handlers/live.ts
@src/db/locations.ts
@src/types/session.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add occupancy aggregation query and dashboard types</name>
  <files>
    src/db/repositories/sessionRepository.ts
    src/types/dashboard.ts
  </files>
  <action>
1. Create `src/types/dashboard.ts` with types:
   - `OccupancyData`: locationId, locationName, latitude, longitude, totalDogs, small, medium, large
   - `ParkDisplay`: extends OccupancyData with optional distanceKm and isUserHere boolean

2. Add `getOccupancyByLocation()` to sessionRepository.ts:
   - Use LEFT JOIN from locations to sessions (ensures parks with 0 dogs appear)
   - Filter sessions by status = 'active'
   - Use PostgreSQL FILTER clause for size-based counting:
     ```sql
     COUNT(sd.dog_id) FILTER (WHERE d.size = 'Small') as small_count
     ```
   - COALESCE counts to 0 for null results
   - Return all 11 locations with their current counts
   - Note: pg returns counts as strings, parse to numbers in mapping

Follow existing sessionRepository.ts patterns for query structure and result mapping.
  </action>
  <verify>
TypeScript compiles without errors: `npx tsc --noEmit`
  </verify>
  <done>
- OccupancyData and ParkDisplay types exported from src/types/dashboard.ts
- getOccupancyByLocation() function exported from sessionRepository.ts
- Function returns all 11 locations with size breakdown counts
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard formatting utilities</name>
  <files>src/bot/utils/dashboard.ts</files>
  <action>
Create `src/bot/utils/dashboard.ts` with formatting functions:

1. `formatSizeBreakdown(small: number, medium: number, large: number): string`
   - Return abbreviated format: "2S, 1M, 2L"
   - Single size simplification per CONTEXT.md: when only one size, show "3 Small dogs" not "3 dogs (3S)"
   - Zero dogs: return "0 dogs"

2. `sortByDogCount(parks: OccupancyData[]): OccupancyData[]`
   - Sort descending by totalDogs
   - Secondary sort by name (alphabetical) for ties

3. `formatDashboard(parks: ParkDisplay[], showEmptyMessage: boolean = true): string`
   - Header: "Live Dog Run Occupancy"
   - Timestamp: "Updated {time}" using Singapore locale (en-SG, hour12: true)
   - If all parks empty AND showEmptyMessage: add "No dogs checked in right now.\n\n"
   - Each park line: "{name}: {sizeBreakdown}"
   - If park.isUserHere: append " [You are here]"
   - If park.distanceKm defined: insert "(X.X km)" after name

4. `buildDashboardKeyboard(currentSort: 'nearest' | 'most_dogs')`
   - Row 1: "Nearest" and "Most Dogs" buttons (callbacks: sort_nearest, sort_most_dogs)
   - Row 2: "Refresh" button (callback: refresh_dashboard)
   - Use Markup.inlineKeyboard from telegraf

Import types from '../types/dashboard.js' and Markup from 'telegraf'.
  </action>
  <verify>
TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
- formatSizeBreakdown handles all size combinations including single-size simplification
- sortByDogCount sorts descending by count with alphabetical tiebreaker
- formatDashboard produces compact output with timestamp and optional "You're here" marker
- buildDashboardKeyboard creates inline keyboard with sort and refresh buttons
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement /live command with dashboard display</name>
  <files>src/bot/handlers/live.ts</files>
  <action>
Replace placeholder in `src/bot/handlers/live.ts`:

1. Import dependencies:
   - getOccupancyByLocation from sessionRepository
   - formatDashboard, sortByDogCount, buildDashboardKeyboard from dashboard utils
   - getActiveSessionByUserId from sessionRepository (for "You're here" marker)
   - findOrCreateUser from userRepository

2. Implement liveHandler:
   - Get user from database (findOrCreateUser with telegram ID)
   - Query active session for user (getActiveSessionByUserId)
   - Get all occupancy data (getOccupancyByLocation)
   - Sort by dog count (sortByDogCount)
   - Map to ParkDisplay[], setting isUserHere = true for user's active session locationId
   - Format dashboard message
   - Reply with message and inline keyboard (sort mode: 'most_dogs')

3. Per CONTEXT.md decisions:
   - Default sort is "Most Dogs" (not Nearest) for initial /live command
   - No dog profile required to view dashboard
   - Show all parks including those with 0 dogs

Note: "Nearest" sort requires location - handled in Plan 02 with reply keyboard flow.
  </action>
  <verify>
1. TypeScript compiles: `npx tsc --noEmit`
2. Start bot: `npm run dev`
3. Send /live command - should show all 11 parks sorted by occupancy with inline buttons
  </verify>
  <done>
- /live command shows all 11 parks with current occupancy counts
- Parks sorted by most dogs (descending) by default
- Size breakdown shown in abbreviated format (2S, 1M, 2L)
- Dashboard includes timestamp
- Inline keyboard shows Nearest, Most Dogs, and Refresh buttons
- User's current check-in location marked with [You are here] if active session
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npx tsc --noEmit` passes with no errors
2. Bot starts successfully with `npm run dev`
3. Send /live to bot:
   - Receive dashboard message with all 11 parks
   - See occupancy counts (likely 0 for all without active sessions)
   - See timestamp in message
   - See inline buttons (Nearest, Most Dogs, Refresh)
4. If you have an active check-in session, see "[You are here]" marker
</verification>

<success_criteria>
- DASH-01: User can view live occupancy via /live command
- DASH-02: Dashboard shows all 11 dog runs with current dog count
- DASH-03: Each park shows size breakdown (abbreviated format: 2S, 1M, 2L)
- DASH-04: Parks sorted by most dogs by default
- Timestamp shows when dashboard was generated
- User's active location highlighted if checked in
</success_criteria>

<output>
After completion, create `.planning/phases/05-live-dashboard/05-01-SUMMARY.md`
</output>
