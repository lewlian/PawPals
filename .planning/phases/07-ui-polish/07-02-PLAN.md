---
phase: 07-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/bot/handlers/start.ts
  - src/bot/handlers/profile.ts
  - src/bot/utils/dashboard.ts
  - src/bot/scenes/checkInWizard.ts
  - src/jobs/sessionExpiry.ts
  - src/bot/handlers/sessionCallbacks.ts
  - src/bot/handlers/checkout.ts
  - src/bot/index.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Dashboard shows emoji prefixes for park names and dog counts"
    - "Check-in confirmation uses emoji format with location, dogs, duration"
    - "Expiry reminder shows clock emoji and includes extend/checkout buttons with emoji"
    - "Profile buttons have emoji prefixes (Edit, Delete, Add, Back)"
    - "Dashboard buttons have emoji prefixes (Refresh, Nearest, Most Dogs)"
  artifacts:
    - path: "src/bot/utils/dashboard.ts"
      provides: "Emoji-formatted dashboard display"
      contains: "formatDashboard"
    - path: "src/bot/scenes/checkInWizard.ts"
      provides: "Emoji-formatted check-in confirmation"
      contains: "EMOJI.checkedIn"
    - path: "src/jobs/sessionExpiry.ts"
      provides: "Emoji-formatted reminder and expiry messages"
      contains: "EMOJI.reminder"
    - path: "src/bot/handlers/profile.ts"
      provides: "Emoji-prefixed profile buttons"
      contains: "EMOJI.edit"
  key_links:
    - from: "src/bot/utils/dashboard.ts"
      to: "src/bot/constants/emoji.ts"
      via: "EMOJI import"
      pattern: "import.*EMOJI.*emoji"
    - from: "src/bot/scenes/checkInWizard.ts"
      to: "src/bot/constants/emoji.ts"
      via: "EMOJI import"
      pattern: "import.*EMOJI.*emoji"
    - from: "src/jobs/sessionExpiry.ts"
      to: "src/bot/constants/emoji.ts"
      via: "EMOJI import"
      pattern: "import.*EMOJI.*emoji"
---

<objective>
Apply consistent emoji styling to all messages and inline buttons throughout the bot.

Purpose: Create visual consistency and improve scannability of bot messages by using standardized emoji prefixes for headers, content sections, and buttons.

Output: All user-facing messages use emoji formatting per CONTEXT.md patterns, all inline buttons have emoji prefixes for visual clarity.
</objective>

<execution_context>
@/Users/sean/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sean/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-polish/07-CONTEXT.md
@.planning/phases/07-ui-polish/07-RESEARCH.md
@.planning/phases/07-ui-polish/07-01-SUMMARY.md
@src/bot/constants/emoji.ts
@src/bot/utils/dashboard.ts
@src/bot/handlers/profile.ts
@src/bot/scenes/checkInWizard.ts
@src/jobs/sessionExpiry.ts
@src/bot/handlers/sessionCallbacks.ts
@src/bot/handlers/checkout.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update message formatting with emoji styling</name>
  <files>
    src/bot/utils/dashboard.ts
    src/bot/scenes/checkInWizard.ts
    src/jobs/sessionExpiry.ts
    src/bot/handlers/sessionCallbacks.ts
    src/bot/handlers/checkout.ts
  </files>
  <action>
Import `EMOJI` from `../constants/emoji.js` (adjust path based on file location) in each file.

**Update src/bot/utils/dashboard.ts:**
- Import `EMOJI` from `../constants/emoji.js`
- Update `formatDashboard()` function:
  - Header: `${EMOJI.live} Live Dog Run Occupancy` (was just "Live Dog Run Occupancy")
  - Each park entry: `${EMOJI.location} ${park.locationName}${distanceStr}\n${EMOJI.dogs} ${formatSizeBreakdown(...)}`
  - Format per CONTEXT.md:
    ```
    📍 West Coast Park
    🐕 5 dogs • 2 Small, 2 Medium, 1 Large
    ```
- Update `formatSizeBreakdown()` to use bullet separator: "5 dogs • 2 Small, 2 Medium, 1 Large"

**Update src/bot/scenes/checkInWizard.ts:**
- Import `EMOJI` from `../constants/emoji.js`
- Import `mainMenuKeyboard` from `../keyboards/mainMenu.js`
- Update check-in confirmation message (in stepDuration action handler):
  ```typescript
  `${EMOJI.checkedIn} Checked in!\n` +
  `${EMOJI.location} ${state.locationName}\n` +
  `${EMOJI.dogs} ${dogNames.join(', ')}\n` +
  `${EMOJI.timer} ${durationMinutes} minutes\n\n` +
  `Auto check-out at ${expiryTime}`
  ```
- Send mainMenuKeyboard with success message to restore reply keyboard after wizard
- Update location confirmed message: `${EMOJI.location} Location confirmed: ${state.locationName}`
- Update geofence failure message to use emoji (optional, low priority)

**Update src/jobs/sessionExpiry.ts:**
- Import `EMOJI` from `../bot/constants/emoji.js`
- Update `formatReminderMessage()`:
  ```typescript
  `${EMOJI.reminder} ${minutesLeft} minutes left!\n` +
  `${session.locationName} • ${dogNamesText}`
  ```
- Update `formatExpiryMessage()`:
  ```typescript
  `${EMOJI.checkedOut} Session ended\n` +
  `${EMOJI.location} ${session.locationName}\n` +
  `${EMOJI.dogs} ${dogNamesText}\n` +
  `${EMOJI.timer} ${durationMinutes} minutes\n\n` +
  `See you next time!`
  ```

**Update src/bot/handlers/sessionCallbacks.ts:**
- Import `EMOJI` from `../constants/emoji.js`
- Update extend confirmation message:
  ```typescript
  `${EMOJI.checkedIn} Session extended!\n` +
  `${EMOJI.location} ${location?.name}\n` +
  `${EMOJI.timer} New end time: ${newExpiryTime}\n\n` +
  `I'll remind you before it expires.`
  ```
- Update checkout confirmation message:
  ```typescript
  `${EMOJI.checkedOut} Checked out!\n` +
  `${EMOJI.location} ${location?.name}\n` +
  `${EMOJI.dogs} ${dogNames}\n` +
  `${EMOJI.timer} ${durationMinutes} minutes\n\n` +
  `See you next time!`
  ```

**Update src/bot/handlers/checkout.ts:**
- Import `EMOJI` from `../constants/emoji.js`
- Update checkout success message to match sessionCallbacks format
- Update "no active session" message with appropriate emoji
  </action>
  <verify>
Run TypeScript compilation: `npx tsc --noEmit`
Start bot locally and test:
1. /live shows dashboard with 📍 and 🐕 prefixes
2. Complete check-in flow - confirmation shows ✅ 📍 🐕 ⏱️ format
3. Wait for reminder - shows ⏰ format with location and dogs
4. /checkout shows ✅ 📍 🐕 ⏱️ format
  </verify>
  <done>
- Dashboard header has 📊 emoji, parks show 📍 location and 🐕 dog count
- Check-in confirmation uses ✅ header with structured emoji details
- Expiry reminders use ⏰ header
- Checkout/extend confirmations use emoji formatting
  </done>
</task>

<task type="auto">
  <name>Task 2: Add emoji prefixes to all inline buttons</name>
  <files>
    src/bot/handlers/profile.ts
    src/bot/utils/dashboard.ts
    src/bot/index.ts
    src/jobs/sessionExpiry.ts
    src/bot/scenes/checkInWizard.ts
  </files>
  <action>
**Update src/bot/handlers/profile.ts:**
- Import `EMOJI` from `../constants/emoji.js`
- Update `buildDogListKeyboard()`:
  - View button: `${EMOJI.dogs} View ${dog.name}` (or just keep as is, dog name is clear)
  - Add button: `${EMOJI.add} Add Another Dog`
- Update `buildDogDetailKeyboard()`:
  - Edit Name: `${EMOJI.edit} Edit Name`
  - Edit Size: `${EMOJI.edit} Edit Size`
  - Edit Breed: `${EMOJI.edit} Edit Breed`
  - Edit Age: `${EMOJI.edit} Edit Age`
  - Delete Dog: `${EMOJI.delete} Delete`
  - Back to List: `${EMOJI.back} Back`
- Update profile messages to include emoji headers where appropriate

**Update src/bot/utils/dashboard.ts:**
- Update `buildDashboardKeyboard()`:
  - Nearest: `📍 Nearest` (active: `📍 Nearest *`)
  - Most Dogs: `🐕 Most Dogs` (active: `🐕 Most Dogs *`)
  - Refresh: `${EMOJI.refresh} Refresh`

**Update src/bot/index.ts:**
- Update inline button callbacks where buttons are defined:
  - Create Dog Profile button (in delete confirmation): `${EMOJI.dogs} Create Dog Profile`
  - Yes, delete: `${EMOJI.delete} Yes, delete`
  - No, keep: `${EMOJI.back} No, keep`

**Update src/jobs/sessionExpiry.ts:**
- Update `buildExtendKeyboard()`:
  - Extend options: `${EMOJI.extend} 15 min`, `${EMOJI.extend} 30 min`, `${EMOJI.extend} 60 min`
  - Checkout: `${EMOJI.checkout} Checkout now`

**Update src/bot/scenes/checkInWizard.ts:**
- Update duration selection buttons:
  - 15 minutes: `⏱️ 15 minutes`
  - 30 minutes (default): `⏱️ 30 minutes ⭐`
  - 60 minutes: `⏱️ 60 minutes`
- Update dog selection buttons (optional - dog names may be clear enough)
- Update "All Dogs" button: `🐕 All Dogs`
  </action>
  <verify>
Run TypeScript compilation: `npx tsc --noEmit`
Start bot locally and verify buttons have emojis:
1. /profile - Edit, Delete, Back buttons have emojis
2. /live - Nearest, Most Dogs, Refresh buttons have emojis
3. Check-in wizard - duration buttons have timer emoji
4. Expiry reminder - Extend and Checkout buttons have emojis
5. Delete confirmation - Yes/No buttons have emojis
  </verify>
  <done>
- Profile detail view shows ✏️ Edit, 🗑️ Delete, ◀️ Back buttons
- Dashboard shows 📍 Nearest, 🐕 Most Dogs, 🔄 Refresh buttons
- Duration selection shows ⏱️ prefix on all options
- Extend keyboard shows ➕ and 🔴 prefixes
- All inline buttons have appropriate emoji prefixes
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npx tsc --noEmit` compiles without errors
2. `npm run dev` starts bot successfully
3. All message types display with emoji formatting:
   - Welcome (from Plan 01)
   - Dashboard
   - Check-in confirmation
   - Expiry reminder
   - Checkout confirmation
   - Session extension confirmation
4. All inline buttons have emoji prefixes:
   - Profile buttons (Edit, Delete, Add, Back)
   - Dashboard buttons (Nearest, Most Dogs, Refresh)
   - Session buttons (Extend, Checkout)
   - Duration selection buttons
   - Delete confirmation buttons
</verification>

<success_criteria>
- UIPOL-03: All messages use consistent emoji prefixes per CONTEXT.md patterns (headers, location pins, dog emoji, timer emoji)
- UIPOL-04: All inline buttons have emoji prefixes for visual clarity (Edit, Delete, Refresh, Extend, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-polish/07-02-SUMMARY.md`
</output>
